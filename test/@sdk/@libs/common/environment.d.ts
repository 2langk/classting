export declare const environment: {
  tz: string;
  node: {
    env: string;
  };
  database: {
    host: string;
    port: number;
    username: string;
    password: string;
    name: string;
  };
  jwt: {
    access: {
      secret: string;
      expire: string;
    };
  };
};
